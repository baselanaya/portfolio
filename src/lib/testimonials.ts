// Real client/colleague testimonials. Intentionally empty until Basel
// supplies genuine quotes — never fabricate reviews.
export interface Testimonial {
  quote: string;
  name: string;
  role: string;
}

export const testimonials: Testimonial[] = [];
